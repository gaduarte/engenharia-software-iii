a. Um cliente não deve ser exposto a métodos dos quais não necessita. Em interfaces extensas, existem diversas operações, no entanto, muitos objetos não fazem uso dessas operações.

b. A manutenção torna-se mais complexa devido aos efeitos colaterais: uma alteração em uma interface nos obriga a modificar classes que não implementam a interface.